import { MSG, STATUS, MISSING_DOCUMENT, INVALID_ID } from './responseConstants';

import ENV from './appConstants';

import MONGOOSE_ERROR from './errorConstants';

export default {
    MSG,
    STATUS,
    MISSING_DOCUMENT,
    INVALID_ID,
    ENV,
    MONGOOSE_ERROR,
};
